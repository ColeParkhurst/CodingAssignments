package codingAssignments;

public class Week3Assignment2 {

	public static void main(String[] args) {
	//question 2 part b	
	/*	String[] names = new String[6];
		
		names[0] = "Sam";
		names[1] = "Tommy";
		names[2] = "Tim";
		names[3] = "Sally";
		names[4] = "Buck";
		names[5] = "Bob";

	/*	int total = 0;
		for (int i = 0; i < names.length; i++) {
			total += names[i].length();
		}
			System.out.println(total / 6);	*/
			
	/*	for (int x = 0; x < names.length; x++) {
			System.out.println(names[x]); 
		}*/
	/*	String firstName = "John";
		String lastName = "Doe";
		String fullName = (firstName + " " + lastName);
		
		System.out.println(fullName); */} 
		//number 9
		/* public boolean test() {
		
		int[]total = new int [5];
		
		total[0] = 13;
		total[1] = 4;
		total[2] = 19;
		total[3] = 29;
		total[4] = 37;
		
		
				
		if (total.length > 100) {
			return true;
		}
		return false; */
	//question 10
	/* double total = 0;
	for (double i = 0; i < example.length; i++) {
		total += example[i];
	}
	System.out.println(total / example.length); */
	/*
	//question 10
	double[]valuesOne = new double[5]; {
	
		valuesOne[0] = 18;
		valuesOne[1] = 6.8;
		valuesOne[2] = 1;
		valuesOne[3] = 4.3;
		valuesOne[4] = 23;
		
	double[]valuesTwo = new double[5];
	
		valuesTwo[0] = 15;
		valuesTwo[1] = 4.9;
		valuesTwo[2] = 3;
		valuesTwo[3] = 5;
		valuesTwo[4] = 22.6;
		
	double totalOne = 0;
	double totalTwo = 0;		
		
	for (int x = 0; x < valuesOne.length; x++) {
		totalOne += valuesOne[x];
	}
	for (int y = 0; y < valuesTwo.length; y++) {
		totalTwo += valuesTwo[y];
		
	}
	if (totalOne / valuesOne.length > totalTwo / valuesTwo.length) {
		return true;	
	}
	else {
		return false: */
	
	
	//this detects which quadrant a point on a graph is in.
	//this is a very easy way to find the location of a point without seeing the graph.
	/*if ( x > 0 && y > 0 ) {
        System.out.println("The point is in quadrant 1.");
    } else if ( x < 0 && y > 0 ) {
        System.out.println("The point is in quadrant 2.");
    } else if ( x < 0 && y < 0 ) {
        System.out.println("The point is in quadrant 3.");
    } else if ( x > 0 && y < 0 ) {
        System.out.println("The point is in quadrant 4.");
    } else {
        System.out.println("The point is resting on an axis."); */
		
		
	}


